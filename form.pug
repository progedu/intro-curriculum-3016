doctype html
html(lang="jp")
  head
    meta(charset="UTF-8")
    title アンケート
  body
    h1 どちらが食べたいですか？
    form(method="post", action=path)
      label(for="name") 名前: 
      input(type="text", name="name", id ="name", placeholder="名前を入力して下さい")
      input(type="radio", name="favorite", id="favorite1", value=firstItem)
      label(for="favorite1") #{firstItem} 
      input(type="radio", name="favorite", id="favorite2",value=secondItem)
      label(for="favorite2") #{secondItem} 
      button(type="submit") 投稿