<!DOCTYPE html>
html(lang="ja")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title アンケートの種類
  body
    h1 そんなURLないよー
    ul
      each item in items
        li
          a.link(href=item.path) #{item.firstItem} or #{item.secondItem}